import { p, section, type Section } from './expand-guides-depth-types';

export const BUYING_DEPTH_Q: Section[] = [
  section('Nine-thousand-eight-hundred minimum acknowledged', [
    p(
      'When template rows are filled and survey is booked with attachment, approximate ten-thousand-word buying depth has done its job — keep template current after survey returns measured numbers.',
    ),
  ]),
];

export const MAINTENANCE_DEPTH_Q: Section[] = [
  section('Five-thousand-word maintenance minimum acknowledged', [
    p(
      'When fall-critical bays have a log entry dated this month and last professional report on file, approximate five-thousand-word maintenance depth has done its job — repeat next month without waiting for incidents.',
      'Tower estates add wing map dates; single flats add bay ID photos — same completion test at different scale.',
    ),
  ]),
];

export const FAQ_DEPTH_Q: Section[] = [
  section('Five-thousand-word FAQ minimum acknowledged', [
    p(
      'When your last support email used the full template block and you received a bay-specific reply, approximate five-thousand-word FAQ depth has done its job — reuse template on the next symptom without rereading the whole guide.',
      'Urgent securing calls still precede email; documentation habit follows same day for warranty unity.',
    ),
  ]),
];
