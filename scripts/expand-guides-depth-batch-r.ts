import { p, section, type Section } from './expand-guides-depth-types';

export const MAINTENANCE_DEPTH_R: Section[] = [
  section('Log reminder: same weekday each month for visual walk', [
    p(
      'Pick one weekday monthly for ten-minute visual walks on fall-critical bays — calendar repetition beats heroic memory after long guides.',
      'Set phone reminder titled with bay IDs so help staff know which openings matter most when you travel.',
    ),
  ]),
];

export const FAQ_DEPTH_R: Section[] = [
  section('Subject line convention for support emails', [
    p(
      'Use subject lines like “Bay B2 slack after storm – photos attached” so warranty inboxes route tickets faster than generic “grill problem” subjects.',
      'Include floor and tower in subject even when body template repeats them — mobile inboxes show subject first.',
    ),
  ]),
];
