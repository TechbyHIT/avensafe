import { p, section, type Section } from './expand-guides-depth-types';

export const BUYING_DEPTH_P: Section[] = [
  section('Template attached equals buying depth complete', [
    p(
      'Long-form buying guidance completes when your survey email attaches the filled bay template — not when word count crosses an arbitrary line on the website.',
    ),
  ]),
];

export const PRICING_DEPTH_P: Section[] = [
  section('Summed spreadsheet equals pricing depth complete', [
    p(
      'Pricing guidance completes when bay lines sum to the total you are about to pay — arithmetic clarity is the five-thousand-word finish line.',
    ),
  ]),
];

export const MAINTENANCE_DEPTH_P: Section[] = [
  section('Joint owner–facility storm walk protocol after named warnings', [
    p(
      'After IMD-named warnings for your district, owners walk fall-critical bays within forty-eight hours when safe; facility staff walk common duct and facade nets the same window.',
      'Each side logs findings with photos to shared or parallel archives — gaps appear when only one party walks and the other assumes all-clear.',
      'Discrepancies trigger professional visit before next routine slot — storm walks are triage, not replacement for gauge visits, but they prevent silent damage from sitting until annual calendar.',
    ),
    p(
      'Children and pets stay off openings until storm walk completes on fall-critical bays — walk first, resume supervised use after log entry says pass or professional clear.',
    ),
  ], {
    callout:
      'Storm walks are free habit; emergency visits after neglected walks are expensive habit.',
  }),
];

export const FAQ_DEPTH_P: Section[] = [
  section('Copy-paste support template block for email clients', [
    p(
      'Locality / tower / floor / bay ID / product type / symptom branch / child or pet use / last service date / dated photo links / recent trades — paste this block, fill blanks, send before calling for non-emergency issues.',
      'Emergency securing still calls first; paste block in follow-up email same day so warranty file matches voice urgency.',
      'Five-thousand-word FAQ depth is achieved when this block lives in sent folder history — not when bookmarked unread.',
    ),
  ], {
    bullets: [
      'Fill every line before non-urgent send',
      'Call first only when immediate securing needed',
      'Same-day email after urgent call',
    ],
  }),
];
